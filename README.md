 PYTHON-Data-Structures---Strings-Tuples
Completed a Python assignment covering String Concatenation, Slicing, Indexing, String Methods, and Tuples. Practiced operations such as joining strings, accessing characters, slicing and reversing strings, using upper(), lower(), capitalize(), count(), and replace(), along with creating, concatenating, repeating, and accessing tuple elements.
PYTHON-Data-Structures--- List, Dictionary, Set & Conditional Statements
1. Lists
- Created lists containing integer and string values.
- Added elements using `append()` and `insert()`.
- Removed elements using `remove()` and `pop()`.
- Added multiple elements using `extend()`.
- Sorted list elements in descending order.
- Used `max()`, `min()`, and `sum()` functions.
- Accessed list elements using positive and negative indexing.
- Used list slicing to retrieve specific elements.
- Displayed list elements in reverse order.

 2. Dictionaries
- Created a dictionary containing student names and marks.
- Accessed values using dictionary keys.
- Added new key-value pairs.
- Updated existing dictionary values.
- Used `keys()`, `values()`, and `items()` methods to access dictionary data.

 3. Sets
- Created sets and observed how duplicate values are automatically removed.
- Learned that sets do not support indexing.
- Created multiple sets and performed set operations.
- Used `union()` to combine unique elements from two sets.
- Used `intersection()` to identify common elements.

 4. Conditional Statements
- Used `input()` to accept a score from the user.
- Converted user input into an integer using `int()`.
- Used `if`, `elif`, and `else` statements.
- Classified performance as Above Average, Average, or Below Average based on the entered score.
- Added customized messages for each performance category.

 Skills Practiced

Python | Lists | Dictionaries | Sets | Indexing | Slicing | Set Operations | User Input | IF-ELIF-ELSE | Conditional Logic

 Conclusion

Through this assignment, I gained hands-on practice with Python data structures and conditional statements. It helped me improve my understanding of storing, accessing, modifying, and analyzing data using basic Python programming concepts.
